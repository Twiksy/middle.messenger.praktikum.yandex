<div class="profile-item">
  <div class="profile-item__wrapper">
    <div class="profile-item__label">
      {{label}}
    </div>
    <input class="profile-item__input" 
    value={{value}}
    type={{type}}
    name={{name}} 
    {{#if readonly}}
      readonly
    {{/if}}
    />
  </div>
</div>
