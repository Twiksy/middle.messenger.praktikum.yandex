<div class="btn {{addClass}}">
	{{#if href}}
		<a href="{{href}}">{{btn-text}}</a>
	{{else}}
		<button  
		{{#if type}}
		type="{{type}}"
		{{else}}
		type="button"
		{{/if}}
		>{{btn-text}}</button>
	{{/if}}
</div>
